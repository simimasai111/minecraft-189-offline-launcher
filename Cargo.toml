[package]
name = "mclaunch"
version = "1.0.0"
edition = "2021"
description = "Minecraft 1.8.9 离线启动器 (Rust) — 配置文件驱动，单文件 exe"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"

[[bin]]
name = "mclaunch"
path = "src/main.rs"
