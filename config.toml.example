# Minecraft 1.8.9 单文件离线启动器配置示例
# 复制为 config.toml 后按需修改（全部可选，缺省即默认值）

# Java 路径解析顺序：
#   1) 这里显式指定的 java（若设置了且是 Java 8）
#   2) 启动器内置的捆绑 JRE（打进 exe 里的 jre/，已自动随包提供）
#   3) 系统 PATH 里的 java
# 默认 "java" 即可：启动器会优先使用内置捆绑 JRE，无需在系统里装 Java。
# 想强制用某个系统 Java 8，写完整路径，例如：
# java = "C:\\Java\\jre1.8.0_421\\bin\\java.exe"
java = "java"

# 离线模式用户名（随便填）
username = "Player"

# 最大内存（MB）
max_ram_mb = 2048
